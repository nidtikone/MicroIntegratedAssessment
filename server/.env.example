# Copy to .env and fill in the real password (never commit .env)
PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string. Replace <db_password> with the real password.
MONGO_URI=mongodb+srv://nearfuture_user:<db_password>@microintegratedassessme.hbmbtgp.mongodb.net/microassessment?retryWrites=true&w=majority&appName=MicroIntegratedAssessment-db

# Comma-separated list of allowed origins for CORS (frontend dev + deployed)
CLIENT_ORIGINS=http://localhost:5173
